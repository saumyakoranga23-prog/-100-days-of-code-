#include <stdio.h>
#include <string.h>
int main() {
    char s[]="Education";
    int count=0;
    for(int i=0;s[i];i++) {
        if(strchr("aeiouAEIOU",s[i])) count++;
    }
    printf("%d",count);
    return 0;
}
